import os
from dotenv import load_dotenv
from pathlib import Path

env_path = Path(__file__).parent / ".env"
load_dotenv(env_path)

db_url = os.getenv("DB_URL")
api_key = os.getenv("API_KEY")

print("DB_URL:", db_url)
print("API_KEY:", api_key)





# Пояснення:
# Паролі, токени та API-ключі не можна зберігати безпосередньо у коді,
# тому що код може бути опублікований у репозиторії (наприклад, GitHub),
# переданий іншим людям або відкритий на чужому комп’ютері.
#
# Якщо секретні дані знаходяться у коді, будь-хто, хто має доступ до файлу,
# може їх побачити та використати.
#
# Використання змінних оточення дозволяє:
# – зберігати конфіденційні дані окремо від коду;
# – легко змінювати налаштування без редагування програми;
# – підвищити безпеку додатку.
#
# Це є стандартною та безпечною практикою у програмуванні.
